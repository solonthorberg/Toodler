import { StyleSheet } from "react-native";
import { lightGray } from "@/src/styles/colors";
import { skyBlue } from "@/src/styles/colors";

export default StyleSheet.create({
    container: {
        flex: 1,
        padding: 16,
        backgroundColor: lightGray,
        alignItems: "center",
        justifyContent: "space-around",
    },
});