import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
    paddingBottom: 80,
  },
  title: {
    fontSize: 32,
    fontWeight: "800",
    marginBottom: 12,
  },
  subtitle: {
    fontSize: 28,
    fontWeight: "800",
    marginBottom: 12,
  },
  emptyState: {
    padding: 32,
    alignItems: "center",
    opacity: 0.6,
  },
  emptyText: {
    fontSize: 16,
    textAlign: "center",
  },
});
